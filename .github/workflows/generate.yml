name: Generate Contributions SVG

on:
  push:
    branches: [main]
    paths:
      - "data/**"
      - "src/**"

  schedule:
    - cron: "0 6 * * *"

  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - name: Sync contributions data
        run: npm run sync
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - run: npm run generate

      - name: Commit updated data and SVG
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/contributions.json contributions.svg
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "stats: update contributions $(date -u +%Y-%m-%d)"
            git pull --rebase
            git push
          fi
